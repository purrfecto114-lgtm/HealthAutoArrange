# HealthAutoArrange 1.1.7

- 更新检查改为每次游戏启动仅执行一次；同一进程内不轮询，也不提供手动重复检查。
- 更新源收敛为 GitHub，移除 CDN/自定义镜像配置与回退路径。
- 更新器改为只提醒：不再下载、暂存、解压或替换任何文件。
- 发现签名验证通过的新版本时显示一次游戏内提醒；F8 版本页可打开 GitHub Release。
- 保留 RSA-SHA256 签名清单校验，并限制请求大小、超时及重定向后的 GitHub HTTPS 主机。
- 补回发布流程所需的公开信任锚文件，并同步 README、发布工作流和静态回归契约。
