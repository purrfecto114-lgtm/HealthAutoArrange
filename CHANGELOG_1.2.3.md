# HealthAutoArrange v1.2.3 — 渲染阶段闪现修复 + 组内强度排序 + 诊断日志刷新

## 一句话总结

v1.2.2 把"mod 顺序"钉死在了每一帧，但用户仍然看到**闪现**：这次不是排序写错，
而是游戏 0.5s 全量重建周期里的两个**渲染阶段**缺陷（旧图标销毁延迟到帧末、
新图标第一帧以不透明预渲染）+ 一个**误导性日志**（看门狗把预期行为当 Warning 刷屏）。
v1.2.3 在游戏自己的重建流程内中和了这两个渲染缺陷，并按用户要求新增
**同组内按效果强度排序**，最后把 F9 诊断日志全面刷新到与当前管线一致。

## 证据链（真实 7.0.1 反编译 + 用户上传的 LogOutput.log）

用户日志（v1.2.2，游戏 7.0.1，BepInEx 5.4.23）关键事实：

- 排序本身**已经稳定**：`AnchoredWriteCount=1`（整个会话仅首轮布局写入），
  `CreationTimePositionCount=33`（创建时定位正常工作），F9 dump 中
  mod 顺序的 x 槽位排列完全正确。
- `Watchdog full refresh: child count drift` 出现 12 次（5 秒节流后）——
  实际是**每轮重建后的预期收尾**（计划标记为"不完整"，下一帧静默补全），
  但旧版把它当 Warning 打印，看起来像故障。
- 由此推断剩余"闪现"不在排序写入面，而在**渲染/动画面**。逐行核对真实
  `MoodleManager/Moodle` 反编译源码后找到两个可由 mod 中和的渲染缺陷：

### 闪现根因 A：重建帧的双渲染（旧排列 + 新排列同帧重叠）

游戏每 0.5s 执行 `ClearMoodles()`（`Object.Destroy` **延迟到帧末**才真正移除）
→ `AddAllMoodles()`（全新 GameObject）。在重建帧内，**旧图标仍在层级里、
仍会渲染**，新图标也已创建并渲染——只要本轮状态集合或布局与上一轮有差异，
用户就会看到旧排列和新排列**重叠一帧**：移位图标出现重影、消失的状态"闪"最后一帧。

### 闪现根因 B：新状态的不透明预渲染帧（Start 延迟一帧）

`AddMoodle` 用 Unity 默认颜色（白、不透明）创建图标 Image；真正设置
淡入透明度的 `Moodle.Start()` **下一帧**才运行。于是每个**新出现**的状态
（`unTransparentTime=0.5`，目标 alpha=0）会先以**完全不透明**渲染一帧，
然后才"消失"再淡入——这就是字面意义的闪现。游戏原版同样存在此现象；
mod 现在在创建当帧预置正确颜色。

## v1.2.3 修复内容

### 1. 重建帧幽灵隐藏（根因 A 修复）
`ClearMoodles` 后缀立即对每个待销毁旧图标 `SetActive(false)`：
- 反编译证明安全性：游戏在 ClearMoodles 之后**从不**再触碰旧节点
  （全程序集无其他调用者；Image/UITooltip/Moodle 均无 OnDisable 逻辑；
  全程序集无 OnTransformChildrenChanged/OnParentChanged 回调）；
  `Object.Destroy` 照常在帧末完成。
- 效果：重建帧只渲染新排列，旧排列/过期状态的重影彻底消失。
- 遵守主开关：mod 停用时完全还原原版行为。

### 2. 创建帧预淡入（根因 B 修复）
`AddMoodle` 后缀按游戏**自己的公式**（`PlayerCamera.main.blackAmount <
PlayerCamera.GetUnconsciousBlack()` 与 `unTransparentTime`，均为程序集公开成员）
预置根/内部 Image 颜色——与 `Moodle.Start()` 下一帧写入的值完全一致，
消除新状态的不透明预渲染帧；弹入动画（y+75、2.5x、淡入）保持原样。

### 3. 同组内按效果强度排序（新功能，默认开启）
- 同一分组内：**按当前效果强度从高到低**排列（游戏图标名末尾的数字，
  如 `pain2`→2、`shock3`→3；强度数据来自 AddMoodle 捕获，末尾数字兜底）。
- 同强度回退规则声明顺序；未知强度排在已知之后；**分组之间的顺序不变**。
- 三种模式可选（F8 设置窗口新增"同组内排序"选择 + 规则文件键 `InGroupSort`）：
  `IntensityDesc`（默认，强度高→低）/ `IntensityAsc`（低→高）/ `RuleIndex`（旧版规则顺序）。
- 旧规则文件无此键时自动应用新默认（强度降序）；设 `RuleIndex` 可完全还原旧行为。

### 4. 状态徽标 DPI 缩放（多分辨率 GUI 一致性）
F8 设置窗口与透明提醒覆盖层自 v1.2.2 起即按屏幕高度做 1x-2x 缩放（1080p 基准），
但左上角状态徽标（HAA: ON/OFF + Ctrl+R/Ctrl+E 热键反馈）是唯一未缩放的 IMGUI 元素
——在 1440p/4K 下渲染为其他 UI 一半的相对尺寸，难以辨认。现统一采用同一
`Screen.height / 1080` 缩放与 `GUI.matrix` 组合（含 F8 打开时右上角定位的虚拟坐标换算），
在 720p（及以下）不缩放、1440p 放大 1.33x、4K 放大 2x；全分辨率实测几何矩阵验证：
窗口上限 70%/80% 屏幕比例、最小尺寸自适应、窄布局 (<600 虚拟宽度) 切换、滚动溢出、
拖拽限制、分辨率热变更每帧自愈。

### 5. 看门狗"预期收尾"与"意外漂移"分流（日志去噪）
每轮重建后的看门狗计划刻意标记为"不完整"（销毁延迟使 childCount 不可知），
下一帧的补全扫描是**设计内行为**：现在静默记录（Debug 级 + 独立计数
`WatchdogFinalize(expected)`），不再以 Warning 刷屏；只有**真正意外**的漂移
（未知重定位、成员消失、容器更换）才保留 Warning（`WatchdogCorrection(unexpected)`）。

### 6. F9 诊断日志全面刷新（符合当前现状）
- 删除过时的 `v1.2.1 stats:` / `v1.2.2 stats:` 分版本统计行 → 单一 `stats:`
  （含全部当前计数：创建时定位、幽灵隐藏、预淡入、看门狗预期/意外）；
- 新增 `frame:`（看门狗计划状态）与 `sort:`（启用状态、组内排序模式、未知策略）行；
- 每条 moodle 行新增 `group=`（所属分组）与 `slot=`（行内槽位序号）；
- **捕获解析修复**：F9 dump 不再受刷新边界 sequence 下限约束——旧版在边界后
  一律打印 `intensity=unknown / name='' / seq=-1`，现在显示真实显示名/强度/
  critical/捕获序号；强度额外回退图标 id 末尾数字（游戏恒可解析）；
- 启动日志与补丁诊断删除过时版本标签，新增组内排序模式行。

## 行为级验证（模拟器 = 真实 7.0.1 地面真相）

`HealthAutoArrange.Sim`（真实 `UnityUiAdapter.cs` 源码 + 忠实假游戏）新增三个场景，
共 **26/26** 通过（双 fake-null 语义）：

- **S10 幽灵隐藏**：重建帧内所有待销毁旧图标必须已停用、新图标全部激活、
  层级不被提前改动、帧末销毁照常完成（含"场景未真正重建即空转通过"的防呆断言）；
- **S11 预淡入**：新状态创建帧根/内部 Image alpha=0（与 Start 公式一致）、
  已有状态 alpha=1、无意识（blackAmount ≥ 阈值）路径公式一致；
- **S12 组内强度排序**：IntensityDesc/IntensityAsc/RuleIndex 三模式 +
  强度变化时组内重排 + 旧版 RuleIndex 行为逐位不变。

回归：xUnit **223/223**（新增 10 个：三模式排序/并列回退/未知强度/空列表兼容/
解析器默认值与告警）；静态冒烟 **103/103**；version_check PASS（1.2.3/1.2.3.0）；
对真实 7.0.1 运行时引用构建 **0 警告 0 错误**。

## 升级与兼容

- 直接覆盖 `BepInEx/plugins/HealthAutoArrange/HealthAutoArrange.Plugin.dll`；
  配置/规则文件无需任何改动（`InGroupSort` 键缺省 = 新默认强度降序）。
- 想要 v1.2.2 及以前的"组内按规则顺序"：F8 → 同组内排序 → 规则顺序，
  或规则文件写 `InGroupSort = RuleIndex`。

## 用户可见变化清单

| 场景 | v1.2.2 | v1.2.3 |
|---|---|---|
| 状态增减/移位时的重建帧 | 旧+新排列重叠闪现一帧 | 只显示新排列 |
| 新状态出现的第一帧 | 不透明闪现后重新淡入 | 直接按淡入曲线出现 |
| 同组内排列 | 规则文件声明顺序 | 当前强度从高到低（可配置） |
| LogOutput.log | 每轮重建一条误导性 Warning | 预期行为静默（Debug 级） |
| F9 诊断 | intensity=unknown/空名/过时统计 | 真实捕获元数据+分组+槽位+当前统计 |
| 状态徽标（1440p/4K） | 固定 12px 不随 DPI 缩放，4K 下过小 | 与 F8 窗口/提醒一致的 1x-2x 缩放 |
